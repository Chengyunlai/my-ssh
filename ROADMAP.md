# MySSH 路线图

本路线图描述方向，不承诺固定发布日期。具体工作以 GitHub Issues 和 Milestones 为准；每次发布时
同步检查本文件与 `CHANGELOG.md`，已经完成的事项进入变更日志，不长期堆积在路线图中。

## Now：1.x 稳定性与工程基线

- 建立 PR CI、分支保护、Issue/PR 模板和 Agent 协作规范。
- 扩充 SSH、SFTP、凭据存储、版本兼容和更新流程的自动化测试。
- 完成 SheetJS 修复包迁移，并持续维护不可信表格预览的大小、复杂度和超时限制。
- 完成 macOS Developer ID 签名与 notarization，验证 zip 自动更新链路。
- 将大型界面和样式文件按稳定领域边界逐步拆分，避免一次性重写。

退出条件：核心链路有回归测试；PR 必须通过 CI 和 Review；三平台发布可重复执行；高风险依赖有明确处置。

## Next：可靠性与插件生态

- 增加 SSH 断线恢复、会话状态诊断和可观测日志。
- 为插件 manifest、兼容矩阵和宿主 API 增加契约测试与示例插件。
- 建立安装包启动冒烟测试、更新回滚说明与发布验收记录。
- 完善键盘导航、无障碍和大文件场景的性能基准。

## Later：可选能力探索

- 在明确安全模型后探索配置导入导出与多设备同步。
- 扩展受治理的官方插件目录及第三方插件开发工具。
- 评估国际化、主题扩展及更细粒度的终端工作区能力。

## 路线图维护规则

1. 新方向先创建 Issue，写明用户问题、非目标和成功指标，再进入路线图。
2. `Now` 只保留当前版本周期能够推进的事项；无法投入的内容下移，不长期悬空。
3. 破坏性变更必须有迁移说明和版本策略。
4. 已发布内容只记录在 `CHANGELOG.md`，路线图不兼任历史日志。
