# MySSH 领域术语

## 插件内容区（Plugin Surface）

由 MySSH 宿主分配给插件面板的唯一可视区域。它位于应用或会话标签页下方，不包含顶栏、
服务器侧栏和底部状态栏；插件可以填满内容区，但不能把内容区扩展到宿主外壳。

## 标准面板（Standard Panel）

使用 `panel.layout: 'standard'` 的轻量插件面板。它适合表单、列表和单一工具流，外层位置、
尺寸和主题由宿主决定。

## 工作台面板（Workspace Panel）

使用 `panel.layout: 'workspace'` 的重型插件面板。它可以在插件内容区内部组织自己的侧栏、
工具栏、标签页和结果区，但这些都是插件内部区域，不是 MySSH 的全局布局区域。

## 宿主 Token

MySSH 注入到插件内容区的稳定语义 CSS 变量。插件通过 Token 使用颜色、间距、圆角和字体，
避免复制或覆盖核心主题；新增 Token 属于核心仓库契约变更。
