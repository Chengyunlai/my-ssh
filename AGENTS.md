# MySSH Agent 协作规范

本文件适用于整个仓库，是 AI Agent 与多人并行开发时的默认工作约定。用户指令、Issue/PR
要求优先于本文件；子目录若存在更具体的 `AGENTS.md`，以距离目标文件最近的规则为准。

## 开始工作前

1. 阅读 `README.md`、`CONTRIBUTING.md` 和 `docs/ARCHITECTURE.md`；插件、UI、发布改动还要分别阅读对应专项文档。
2. 明确任务的目标、非目标、验收条件和预计修改文件。信息不足时先调查，不能用大范围重构代替澄清。
3. 运行 `git status --short`。工作区中的既有修改属于其他开发者，不得覆盖、暂存、回滚或顺手格式化。
4. 优先关联一个 Issue。小型维护任务至少要在 PR 描述中写清问题、范围与验证方式。

## 多人 / 多 Agent 并行规则

- 一个执行者只领取一个边界清晰的子任务；开始前声明负责的目录或文件。
- 两个执行者不得同时修改同一文件。必须共享文件时，指定唯一负责人，由其他人提供建议或补丁片段。
- 独立任务使用独立分支或 worktree；分支命名遵循 `type/short-description`，工具要求前缀时保留工具前缀。
- 不得执行 `git reset --hard`、`git clean`、强制推送或删除他人分支；不得使用 stash 隐藏共享工作区修改。
- 未经明确授权，不提交、推送、创建 PR 或修改远端仓库设置。
- 发现任务范围重叠、接口变化或潜在冲突时立即同步，不在最后合并阶段才暴露。

## 模块边界

| 路径 | 职责 | 约束 |
| --- | --- | --- |
| `src/main/` | Electron 主进程、SSH/SFTP、存储、更新 | 文件系统、网络、凭据等高权限能力只能放这里 |
| `src/preload/` | IPC 与 `contextBridge` 边界 | 只暴露最小、类型化 API，不透传 Node 能力 |
| `src/renderer/` | React UI、终端与插件界面 | 只能通过 `window.ssh.*` 使用主进程能力 |
| `src/shared/` | 跨进程类型和纯逻辑 | 不依赖 Electron、DOM 或具体进程实现 |
| `src/renderer/src/plugins/` | 内置插件 | 遵循 `docs/PLUGIN.md`，保持宿主 API 边界 |

跨边界修改必须同步更新 `src/shared/types.ts`、preload 实现和相关调用方。不要复制跨进程类型，
不要让 renderer 直接导入 `src/main/`。

## 实现与验证

- 保持变更最小且围绕一个目的；禁止顺手重写无关文件。
- 新行为应在可测试的纯逻辑边界补充测试，Bug 修复优先添加回归测试。
- 新依赖必须说明必要性、安全影响与体积影响，并提交 `package-lock.json`。
- 涉及凭据、远程文件、IPC、插件加载或更新流程时，必须评估不可信输入与最小权限。
- 完成前运行 `make check`；若受平台或环境限制无法运行某项检查，交接时明确说明。

## Issue、PR 与交接

- Issue 应包含背景、范围、非目标、验收条件、风险和依赖关系；可并行子任务应明确文件边界。
- PR 应关联 Issue，说明“改了什么 / 为什么 / 如何验证”，UI 改动附截图，破坏性变更单独标注。
- 提交信息遵循 Conventional Commits；一次提交只表达一个逻辑变更。
- 交接内容必须包含：完成摘要、修改文件、验证命令与结果、遗留风险、下一步建议。

## 完成定义

- 验收条件全部满足，没有偷偷扩大的范围。
- lint、类型检查、测试和构建通过。
- 用户可见行为、架构边界或发布方式变化时，相应文档已同步。
- 没有包含密钥、凭据、真实服务器信息或本地生成产物。
- `git diff` 已自查，且未混入其他开发者的修改。
